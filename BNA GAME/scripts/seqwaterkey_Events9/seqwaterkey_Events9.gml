// Auto-generated stubs for each available event.

function seqwaterkey_Moment()
{
	room_goto(rStart)
	spawn_x = 7100;
	spawn_y = 250;
	oPlayer.x = spawn_x; // Directly update oPlayer's x
	oPlayer.y = spawn_y; // Directly update oPlayer's y
	global.keynum =+1;
	if (audio_is_playing(sndMusic)){
		audio_play_sound(sndMusic, 1, true);
		}
}



function seqelixirkey_Moment()
{
	room_goto(rStart)
	spawn_x = 7100;
	spawn_y = 250;
	oPlayer.x = spawn_x; // Directly update oPlayer's x
	oPlayer.y = spawn_y; // Directly update oPlayer's y
	global.keynum =+1;
	if (audio_is_playing(sndMusic)){
		audio_play_sound(sndMusic, 1, true);
		}
}